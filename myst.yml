version: 1
project:
  title: Molecular Simulations
  id: mol-sim-2025
  exclude:
    - venv
    - '**/venv'
    - _build

site:
  template: book-theme
  options:
    # MOVED: The error log specifically asked for this nesting
    base_url: /molsim