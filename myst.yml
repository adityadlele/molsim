version: 1
project:
  title: Molecular Simulations
  id: mol-sim-2025
  # 1. Clean TOC: We only want your actual file here
  toc:
    - file: README.md
  # 2. Ignore Rule: This prevents it from looking in venv ever again
  exclude:
    - venv
    - '**/venv'
    - _build

site:
  template: book-theme
  options:
    # 3. The Style Fix: Don't forget this!
    base_url: /molsim