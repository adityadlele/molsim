version: 1
project:
  title: Molecular Simulations
  id: mol-sim-2025
  # FIX: We must explicitly tell it which file is the homepage
  index: README.md
  exclude:
    - venv
    - '**/venv'
    - _build

site:
  template: book-theme
  options:
    base_url: /molsim